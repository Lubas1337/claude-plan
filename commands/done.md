---
description: "Пометить часть как done, каскадно обновить blocked→ready, записать в CHANGELOG"
---

# /claude-plan:done — Завершение части

## Инструкции

> При вопросах пользователю — используй `AskUserQuestion` tool с опциями (селектор), а не текстовые вопросы.

Помечает часть как `done`, каскадно разблокирует зависимые части, пишет CHANGELOG. Skill "plan" содержит форматы и каскадную логику. Используй его как справку.

### Входные данные

Аргумент: `$ARGUMENTS` — план и номер части.
- `/claude-plan:done ai-features 03` — завершить часть 03
- `/claude-plan:done ai-features` — завершить текущую in_progress часть
- `/claude-plan:done ai-features 03 --yolo` — завершить и продолжить yolo-цикл

### Алгоритм

#### 1. Проверки

1. Прочитай META.md части
2. Проверь что все задачи `done` или `skipped`
3. Если есть незавершённые задачи:

   **С --yolo:** НЕ завершай — это аномалия, останови цикл:
   ```
   ⛔ YOLO stopped: незавершённые задачи в Part {NN}: {Name}
   ```

   **Без --yolo:** предупреди и спроси через `AskUserQuestion` с опциями:
   - "Завершить" — пометить часть как done несмотря на незавершённые задачи
   - "Вернуться к задачам" — не завершать, продолжить работу

#### 2. Обновление статуса

1. META.md: `Status: done`
2. MASTER.md: обнови статус в таблице

#### 3. Каскадное разблокирование

Для каждой `blocked` части:
1. Проверь все её зависимости
2. Если ВСЕ зависимости `done` (или `skipped`) → смени на `ready`
3. Обнови META.md разблокированной части
4. Обнови MASTER.md

#### 3.5 Предложение запуска агентов

После разблокирования, проверь новые ready части:

Если есть ready части с `Agent:` != `none`:

**С --yolo:** автоматически запусти агентов для ВСЕХ разблокированных частей с Agent != none (процедура "Parallel Agent Launch" из SKILL.md). Не спрашивай подтверждения.

**Без --yolo:**

Покажи список разблокированных частей с агентами, затем спроси через `AskUserQuestion` с опциями:
- "Запустить всех" — запустить агентов для всех разблокированных частей
- По одной опции на каждую часть (например "05-monitoring", "06-deployment") — запустить агента только для выбранной
- "Пропустить" — не запускать агентов

Используй `multiSelect: true` чтобы пользователь мог выбрать несколько частей.

- Если пользователь выбрал части → процедура "Launch Agent" или "Parallel Agent Launch" из SKILL.md
- Если пропустил → просто показать `Next ready:` как обычно

НЕ запускай агентов автоматически без подтверждения пользователя.

#### 4. CHANGELOG

Добавь запись в начало CHANGELOG.md:

```markdown
## v0.N.0 — YYYY-MM-DD
### Part NN: Name — done
- Краткое описание что сделано (из task results)
- Ссылки на ADR если есть
```

Номер версии = количество done частей.

#### 5. Обновление дашбордов

1. STATUS.md — пересчитай прогресс
2. INDEX.md — обнови строку плана

#### 6. Проверка завершения плана

Если ВСЕ части `done` или `skipped`:
1. INDEX.md: статус плана → `completed`
2. CHANGELOG.md: версия `v1.0.0`
3. Поздравь пользователя

#### 6.5 Обновление PROJECT.md

Если `.plan/PROJECT.md` существует и план завершён:
1. Обнови статус плана в Roadmap таблице
2. Если ВСЕ планы фазы completed → статус фазы → `done`

#### 7. Генерация GUIDE.md

Если план завершён (шаг 6 сработал):

1. **Собери данные**:
   - Прочитай MASTER.md: Vision, Goals, Constraints, Out of Scope
   - Прочитай CHANGELOG.md: summary по каждой части
   - Прочитай все `parts/*/META.md`: Goal, Files Affected
   - Прочитай все task files (`parts/*/NN-task.md`) не-skipped частей: Verification, Acceptance Criteria, Result
   - Проверь наличие `research/adr/` — если есть, прочитай ADR файлы

2. **Создай `.plan/<plan-name>/GUIDE.md`** по формату из SKILL.md

3. **Обнови INDEX.md**: добавь ссылку `[GUIDE](plan-name/GUIDE.md)` в колонку Guide

4. Покажи пользователю:
   ```
   📖 GUIDE.md создан: .plan/<plan-name>/GUIDE.md
   ```

### Вывод

```
✅ Part 03: API Layer — done

Unblocked:
  → 05-monitoring (ready)
  → 06-deployment (ready)

Changelog: v0.3.0 added

Progress: 3/7 done (43%)
▓▓▓▓░░░░░░ 43%

Next ready: 04-workers (S), 05-monitoring (M)
```

При завершении плана (шаг 6+7) добавить:
```
📖 Guide: .plan/<plan-name>/GUIDE.md
```
